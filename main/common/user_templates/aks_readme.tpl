### aks cluster connect command
- `az aks get-credentials --resource-group ${rg_name} --name ${aks_name}`

### publicIP 
- `${pip}`

### rancher url
- `rancher.${pip}.nip.io`

### kong manager url
- `kongmgr.${pip}.nip.io`
